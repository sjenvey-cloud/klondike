package com.cardgames.server.deck;

/**
 * Decommissioned — replaced by POST /api/v1/hands (HandController).
 *
 * The Deck entity and deck table are retained for historical data only.
 * All new game hands use the seeded shuffle model (DEV-67/68/73).
 */
public class DeckController {
    // Intentionally empty — no endpoints exposed.
}
