package com.cardgames.server.deal;

/**
 * Decommissioned — replaced by /api/v1/sessions (SessionController).
 *
 * The Deal entity and deal table are retained for historical data only.
 * All new game sessions use the hands/sessions model (DEV-67–73).
 */
public class DealController {
    // Intentionally empty — no endpoints exposed.
}
